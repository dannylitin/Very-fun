# Port the proxy listens on (defaults to 3000)
PORT=3000

# Comma-separated list of origins allowed to call this proxy.
# Leave blank to allow all origins (suitable for a public demo tool).
# Example: ALLOWED_ORIGINS=https://publictype.co,https://www.publictype.co
ALLOWED_ORIGINS=
